# 7. COMENTARIO EN ESPAÑOL:
# Este es un archivo de ejemplo para tus variables de entorno locales.
# RENÓMBRALO a .env.local (sin el .example) y completa los valores.
# NO subas el archivo .env.local a GitHub, ya que contiene información sensible.
# El archivo .env.local ya está en tu .gitignore.

# URL de conexión a tu base de datos PostgreSQL en Google Cloud.
# Formato general: postgresql://USUARIO:CONTRASEÑA@HOST:PUERTO/NOMBRE_BASE_DE_DATOS?schema=public
#
# Para Cloud SQL Auth Proxy (RECOMENDADO PARA DESARROLLO LOCAL):
# DATABASE_URL="postgresql://TU_USUARIO_DB:TU_CONTRASEÑA_DB@localhost:5432/TU_NOMBRE_DB?host=/cloudsql/TU_PROYECTO_ID:TU_REGION:TU_INSTANCIA_ID&schema=public"
# Asegúrate de que el Cloud SQL Auth Proxy esté corriendo y escuchando en localhost:5432 y que mapee a tu instancia.
#
# Para conexión directa (requiere configurar IP pública y firewall en Cloud SQL, menos seguro para desarrollo):
# DATABASE_URL="postgresql://TU_USUARIO_DB:TU_CONTRASEÑA_DB@IP_PUBLICA_DE_TU_INSTANCIA:5432/TU_NOMBRE_DB?sslmode=prefer&schema=public"
#
# Reemplaza los placeholders (TU_USUARIO_DB, TU_CONTRASEÑA_DB, etc.) con tus credenciales reales.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# URL de tu aplicación (usada en algunos fetches del lado del servidor si es necesario)
# En desarrollo, si tu app corre en el puerto 9002, sería:
NEXT_PUBLIC_APP_URL="http://localhost:9002"
