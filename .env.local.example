# Prisma
# Replace with your actual database connection string from Google Cloud SQL (PostgreSQL)
# Example for a direct connection (ensure your GCP firewall rules allow it, or use Cloud SQL Proxy for local dev):
# DATABASE_URL="postgresql://USER:PASSWORD@PUBLIC_IP_ADDRESS:5432/DATABASE_NAME?sslmode=prefer"
#
# Example for Cloud SQL Proxy (run proxy locally mapping to localhost:5432):
# DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/DATABASE_NAME?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME"
#
# For production on Google Cloud (e.g., Cloud Run), you might use Unix sockets:
# DATABASE_URL="postgresql://USER:PASSWORD@localhost/DATABASE_NAME?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME"

DATABASE_URL="postgresql://your_db_user:your_db_password@your_db_host:5432/your_db_name"

# Next.js App URL (if needed for API calls from server components in specific scenarios, or for metadata base URL)
# Ensure this matches the port you are running on locally or your production URL
NEXT_PUBLIC_APP_URL="http://localhost:9002"
